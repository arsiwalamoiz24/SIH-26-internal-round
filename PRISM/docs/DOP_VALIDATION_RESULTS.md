# DOP_VALIDATION_RESULTS — merged into DOP_VALIDATION.md

This file's content (pipeline validation + the final resolved candidate-specific DOP results) has been merged into **[`DOP_VALIDATION.md`](./DOP_VALIDATION.md)**, along with the earlier blocked-coverage investigation that used to live in a separate file of the same name. Read that file for the complete chronological story (Part 1: blocked → Part 2: pipeline validated on a non-candidate acquisition → Part 3: resolved with the real covering acquisition).

Kept as a redirect stub, not deleted, because several real pipeline output files (`outputs/objective1/dop/dop_validation_results.json`, `candidate_dop.json`, `PHYSICS_RESULTS.json`, `physics_evidence_score.json`, and others) cite this filename as a source in their provenance metadata — those citations still resolve correctly to this path.
